# v0.2.0 Phase 3 Build Log: BA Document Workflows

Date: 2026-05-23

## Scope

Implemented the document workflow set from `builder/plan/PLAN-v0.2.0.md`.

## Runtime Files Added

- `bmad-ba-document-concept/`
- `bmad-ba-template-generator/`
- `bmad-ba-document-writer/`
- `bmad-ba-document-review/`
- `bmad-ba-document-merge/`

Each workflow includes `SKILL.md`, `workflow.md`, and step files.

## Runtime Registration Updated

- Added Nora menu entries:
  - `BC` - BA Document Concept
  - `BT` - BA Template Generator
  - `BW` - BA Document Writer
  - `BV` - BA Document Review
  - `BM` - BA Document Merge
- Added all document workflows to `module-help.csv`.
- Updated `module.yaml` to version `0.2.0`.
- Updated `README.md` to describe v0.2.0 scope.

## Design Notes

- The original plan suggested `BD` and `BR` for document workflows, but those conflict with existing BA Discovery and BA Requirements codes.
- The implemented menu codes avoid conflicts while keeping related document workflow codes grouped.
- Document workflows remain independent from knowledge learning unless the user explicitly requests reuse, citation, traceability, or conflict checking.
- `.docx` export remains out of scope; merge workflow creates Markdown and optional export notes only.

## Next Module-Builder Step

Run validation and record v0.2.0 readiness.

## Validation

- `bmad-ba-document-concept` passed targeted strict validation.
- `bmad-ba-template-generator` passed targeted strict validation.
- `bmad-ba-document-writer` passed targeted strict validation.
- `bmad-ba-document-review` passed targeted strict validation.
- `bmad-ba-document-merge` passed targeted strict validation.
- Updated `bmad-agent-ba-assistant` passed targeted strict validation.
- The standard BMAD validator file was not changed. Repository-level validation remains governed by the upstream checkout state; custom module readiness is based on targeted validation for the module skills.
