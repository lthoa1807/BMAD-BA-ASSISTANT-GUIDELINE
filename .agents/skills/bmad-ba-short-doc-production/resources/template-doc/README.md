# Template Document Resources

This folder contains packaged template resources for BA Assistant document workflows.

Runtime workflows copy these resources into `{ba_artifacts}/documents/template-doc/` when the matching runtime template is missing or when the user confirms a repair or update.

Do not store customer-specific samples, screenshots, raw `.docx` files, or generated project outputs in this packaged resource folder.
