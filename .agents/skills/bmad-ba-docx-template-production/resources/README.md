# DOCX Template Production Resources

This folder contains the reusable JSON template shape and a minimal sample template for `bmad-ba-docx-template-production`.

Use `template-schema.json` as the supported capability contract for templates. Use `sample-template.json` and `sample-content.json` as a starting point when creating a new template manually or when validating the generator behavior.
